# Usage
```
git clone https://github.com/yukidarake/eslint-example.git
cd eslint-example
npm i

# テスト実行
npm test

# 実行
node_modules/.bin/eslint --reset --config ./.eslintrc --rulesdir lib/rules/  test.js
```

